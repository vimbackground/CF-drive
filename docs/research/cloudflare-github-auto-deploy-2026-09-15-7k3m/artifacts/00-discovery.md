# Discovery sweep

## queries_and_tools

- Cloudflare official documentation web search: Workers Builds GitHub automatic deployment.
- Cloudflare official documentation web search: D1 create, database ID and binding.
- Cloudflare official documentation web search: R2 bucket creation and Worker binding.
- Cloudflare official documentation web search: Worker secrets and build configuration.

## new_vocabulary

- Workers Builds, Git integration, production branch, non-production branch builds, build command, deploy command, runtime secrets, automatic R2/D1 provisioning, and bindings.

## new_entities

- Cloudflare Workers Builds: native GitHub/GitLab connected CI/CD.
- Wrangler: creates R2/D1 resources and deploys the Worker.
- Cloudflare Workers and Pages GitHub App: repository authorization surface.

## gaps

- Account-specific Dashboard labels and permissions can differ by plan and organization policy; the tutorial records navigation paths and official concepts, not UI screenshots.

## negative_results

- No official source found that makes a static D1 binding valid without a D1 ID allocated during resource creation.

## next_queries

- No deep-research escalation: first-party docs directly cover deployment, binding, secrets and rollback.
